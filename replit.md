# Replit Notes

The primary project documentation lives in `README.md`.

This file is intentionally kept minimal so GitHub and other repository viewers
use the README as the source of truth. Keep only Replit-specific setup notes
here.

## Replit Context

- Frontend artifact: `artifacts/careerpath-ai`
- API artifact: `artifacts/api-server`
- Frontend dev port: `21588`
- API dev port: `8080`

Use the commands and environment variable guidance in `README.md` for local
setup, typechecking, builds, and development servers.
